<div class="nested-fields">
  <table>
    <tr>
      <td>image</td>
      <td><%= f.collection_select(:material_id, @materials, :id, :name, prompt: "選択してください") %></td>
      <td><%= "在庫表示" %></td>
      <td><%= "単位" %></td>
      <td><%= f.number_field :use_material_number %></td>
      <td><%= link_to_remove_association "削除", f %></td>
    </tr>
  </table>
</div>